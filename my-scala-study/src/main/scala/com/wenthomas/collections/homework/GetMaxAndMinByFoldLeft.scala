package com.wenthomas.collections.homework

/**
 * @author Verno
 * @create 2020-03-07 23:17 
 */
/**
 * 使用 foldLeft 同时计算最大值和最小值(一次折叠完成)
 */
object GetMaxAndMinByFoldLeft {
    private val list1 = List(30, 5, 7, 60, 1, 20)

    //list1.foldLeft(Array(0, 0))((arr, i) => if ())
}
